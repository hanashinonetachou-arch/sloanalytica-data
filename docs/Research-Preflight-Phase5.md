# Research Preflight Phase 5

詳細Researchの前に最大5機種だけ浅く確認するusage節約ゲート。

判定:
- GO: 本調査へ
- REVIEW: 追加の浅い確認が必要
- HOLD: 現時点では本調査しない

Preflight数値はMachineDataへ直接採用禁止。本調査で複数ソース照合する。

`npm run preflight:validate`
`npm run preflight:status`
`npm run preflight:apply`
`npm run machines:score`
