import fs from"node:fs";
export function validatePreflight(d){const errors=[],keys=new Set();if(d?.schemaVersion!=="research-preflight-v1")errors.push("invalid schemaVersion");if((d?.candidates??[]).length>(d?.policy?.maxCandidatesPerBatch??5))errors.push("batch exceeds maxCandidatesPerBatch");for(const c of d?.candidates??[]){if(keys.has(c.marketKey))errors.push(`duplicate ${c.marketKey}`);keys.add(c.marketKey);if(!["GO","REVIEW","HOLD"].includes(c.decision))errors.push(`${c.marketKey}: invalid decision`);for(const k of["researchReadiness","platformFit","estimatedWorkload","settingInferenceValue"])if(!["UNKNOWN","LOW","MEDIUM","HIGH"].includes(c[k]))errors.push(`${c.marketKey}: invalid ${k}`)}return{ok:!errors.length,errors}}
if(import.meta.url===`file://${process.argv[1]}`){const r=validatePreflight(JSON.parse(fs.readFileSync(process.argv[2]??"research-preflight.json","utf8")));if(!r.ok){for(const e of r.errors)console.error("ERROR:",e);process.exit(1)}console.log("OK: Research Preflightを検証しました")};