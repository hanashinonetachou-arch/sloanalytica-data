import fs from"node:fs";import path from"node:path";import{fileURLToPath}from"node:url";const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),".."),registry=JSON.parse(fs.readFileSync(path.join(ROOT,"machine-registry.json"),"utf8")),presence=JSON.parse(fs.readFileSync(path.join(ROOT,"market-presence-snapshot.json"),"utf8")),included=new Set((registry.machines??[]).filter(m=>m.appStatus==="INCLUDED").map(m=>m.machineId)),rows=(presence.machines??[]).filter(p=>!p.registryMachineId||!included.has(p.registryMachineId)).map(p=>({...p,priority:p.installCountRank<=10?"HIGH":p.installCountRank<=20?"MEDIUM":"LOW"}));console.log(`現在設置上位・未収録候補: ${rows.length}`);for(const r of rows)console.log(`${r.priority}\t#${r.installCountRank}\t${r.displayName}\t導入率:${r.installationRatePercent}%\t傾向:${r.sevenDayTrend}`);