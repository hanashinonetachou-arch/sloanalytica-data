import fs from"node:fs";
export function validateUniverse(d){const errors=[],keys=new Set();if(d?.schemaVersion!=="active-machine-universe-v1")errors.push("invalid schemaVersion");for(const m of d?.machines??[]){if(!m.universeKey||keys.has(m.universeKey))errors.push(`duplicate/empty universeKey: ${m.universeKey}`);keys.add(m.universeKey);if(!m.displayName)errors.push(`${m.universeKey}: displayName required`);if(!["ACTIVE_CONFIRMED","ACTIVE_CANDIDATE","SCHEDULED","UNCERTAIN","RETIRED"].includes(m.lifecycleStatus))errors.push(`${m.universeKey}: invalid lifecycleStatus`);if(m.presenceRank!=null&&(!Number.isInteger(m.presenceRank)||m.presenceRank<1))errors.push(`${m.universeKey}: invalid presenceRank`)}return{ok:!errors.length,errors}}
if(import.meta.url===`file://${process.argv[1]}`){const r=validateUniverse(JSON.parse(fs.readFileSync(process.argv[2]??"active-machine-universe.json","utf8")));if(!r.ok){for(const e of r.errors)console.error("ERROR:",e);process.exit(1)}console.log("OK: Active Machine Universeを検証しました")};