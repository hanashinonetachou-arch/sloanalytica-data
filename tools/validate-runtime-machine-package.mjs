import fs from "node:fs";import path from "node:path";import {validateMachineData} from "./audit-public-data.mjs";
const files=process.argv.slice(2);if(!files.length)throw new Error("Usage: node tools/validate-runtime-machine-package.mjs FILE...");
let failed=false;for(const file of files){const p=path.resolve(file),data=JSON.parse(fs.readFileSync(p,"utf8"));const result={root:process.cwd(),errors:[],warnings:[]};validateMachineData(data,result,data.machine?.machineId??path.basename(path.dirname(p)),p);for(const x of [...result.errors,...result.warnings])console.log(`${x.severity.toUpperCase()} [${x.scope}] ${x.message}`);if(result.errors.length){failed=true;console.log(`FAIL: ${p} (${result.errors.length} errors)`);}else console.log(`PASS: ${p} (${result.warnings.length} warnings)`);}if(failed)process.exitCode=1;
