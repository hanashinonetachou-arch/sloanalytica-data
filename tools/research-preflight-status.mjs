import fs from"node:fs";const p=JSON.parse(fs.readFileSync("research-preflight.json","utf8"));for(const d of["GO","REVIEW","HOLD"]){const rows=(p.candidates??[]).filter(x=>x.decision===d);console.log(`${d}: ${rows.length}`);for(const x of rows)console.log(`  ${x.displayName} | 情報:${x.researchReadiness} Fit:${x.platformFit} 工数:${x.estimatedWorkload} 推測価値:${x.settingInferenceValue}`)}