import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';import {buildNumericInferenceProfile} from '../tools/build-numeric-inference-profile.mjs';
test('Im Juggler EX Difficulty data is exact and MachineData-aligned',()=>{const r=JSON.parse(fs.readFileSync('research/S_IM_JUGGLER_EX_TP/research-data.json'));const s=JSON.parse(fs.readFileSync('research/S_IM_JUGGLER_EX_TP/selection-data.json'));const d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:400});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.includedNumericFeatureCount,1);assert.equal(d.coverage.analyzableFeatureCount,1);const p=buildNumericInferenceProfile(r,s);assert.equal(p.profile,'LIMITED');const grape=p.rejectedFeatures.find(x=>x.featureId==='FEAT_IM_GRAPE_CANDIDATE');assert.equal(grape.rejectionReason,'解析値ではなく実戦値。');});