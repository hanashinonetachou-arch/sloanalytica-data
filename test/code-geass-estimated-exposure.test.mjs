import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Code Geass scores exact small-role exposure and dependence-safe derived cherry-RB exposure',()=>{const r=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/research-data.json')),s=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:200});assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);const cond=s.features.find(f=>f.featureId==='FEAT_CHERRY_RB_GIVEN_CHERRY');assert.equal(cond?.difficultyExposure?.quality,'DERIVED');assert.equal(cond?.difficultyExposure?.sourceCategoryId,'CAT_1_INP_CHERRY_COUNT');});