import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';test('Revue ESTIMATED exposures are score eligible',()=>{const r=JSON.parse(fs.readFileSync('research/S_REVUE_STARLIGHT_CX/research-data.json')),s=JSON.parse(fs.readFileSync('research/S_REVUE_STARLIGHT_CX/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:300});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,6);assert.equal(d.scoreConfidence.level,'MEDIUM');});