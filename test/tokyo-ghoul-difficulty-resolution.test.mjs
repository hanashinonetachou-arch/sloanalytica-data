import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Tokyo Ghoul game-based Difficulty uses only defensible game-exposure Features',()=>{const r=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/research-data.json')),s=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:250});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);const excluded=s.features.filter(f=>['FEAT_AT_RETURN','FEAT_CZ_WITHIN_100'].includes(f.featureId));assert.ok(excluded.every(f=>f.difficultyParticipation==='EXCLUDE'));});