import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Tokyo Ghoul estimated game basis produces score with LOW confidence',()=>{const r=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/research-data.json')),s=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:250});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,4);assert.equal(d.targetGameBasis.quality,'ESTIMATED');assert.equal(d.scoreConfidence.level,'LOW');});