import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Bakemonogatari scores all three adopted numeric Features with explicit estimates',()=>{const r=JSON.parse(fs.readFileSync('research/L_SMASLO_BAKEMONOGATARI_KH/research-data.json')),s=JSON.parse(fs.readFileSync('research/L_SMASLO_BAKEMONOGATARI_KH/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:300});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.inferenceNumericFeatureCount,3);assert.equal(d.coverage.analyzableFeatureCount,3);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,0);assert.equal(d.scoreConfidence.level,'LOW');});