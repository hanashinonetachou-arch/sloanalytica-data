import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
const read=p=>JSON.parse(fs.readFileSync(p));
test('Code Geass scores dependence-safe conditional cherry-RB and excludes non-game opportunities',()=>{const s=read('research/S_CODE_GEASS_3_CC_FS/selection-data.json');const d=evaluateMachineDifficulty(read('research/S_CODE_GEASS_3_CC_FS/research-data.json'),s,{simulationsPerSetting:200});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);assert.equal(d.coverage.blockedDifficultyExposureFeatures.length,0);const cond=s.features.find(f=>f.featureId==='FEAT_CHERRY_RB_GIVEN_CHERRY');assert.equal(cond?.difficultyExposure?.mode,'derived_event_rate');assert.equal(cond?.denominatorInputId,'INP_CHERRY_COUNT');});
test('Tokyo Ghoul excludes conditional opportunities that cannot be uniquely derived from games',()=>{const d=evaluateMachineDifficulty(read('research/L_TOKYO_GHOUL/research-data.json'),read('research/L_TOKYO_GHOUL/selection-data.json'),{simulationsPerSetting:200});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);assert.equal(d.coverage.blockedDifficultyExposureFeatures.length,0);});