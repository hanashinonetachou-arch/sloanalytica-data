import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';test('Mushoku Shirone remains inference-only for game-based Difficulty',()=>{const r=JSON.parse(fs.readFileSync('research/L_MUSHOKU_TENSEI_NM/research-data.json')),s=JSON.parse(fs.readFileSync('research/L_MUSHOKU_TENSEI_NM/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:300});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,1);const shirone=s.features.find(f=>f.featureId==='FEAT_SHIRONE_KINGDOM_TRANSITION_RATE');assert.equal(shirone.difficultyParticipation,'EXCLUDE');});