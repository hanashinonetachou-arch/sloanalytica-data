import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';import {buildNumericInferenceProfile} from '../tools/build-numeric-inference-profile.mjs';
test('Mushoku Difficulty derives Hitogami exposure and explicitly excludes Shirone',()=>{const r=JSON.parse(fs.readFileSync('research/L_MUSHOKU_TENSEI_NM/research-data.json'));const s=JSON.parse(fs.readFileSync('research/L_MUSHOKU_TENSEI_NM/selection-data.json'));const d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:400});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.inferenceNumericFeatureCount,3);assert.equal(d.coverage.includedNumericFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,1);assert.equal(d.coverage.analyzableFeatureCount,2);const p=buildNumericInferenceProfile(r,s);assert.equal(p.profile,'NORMAL');});