import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';test('v1.2 exposes ESTIMATED and confidence',()=>{const r=JSON.parse(fs.readFileSync('research/S_REVUE_STARLIGHT_CX/research-data.json')),s=JSON.parse(fs.readFileSync('research/S_REVUE_STARLIGHT_CX/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:100});assert.equal(d.analyzerVersion,'difficulty-analyzer-v1.2');assert.ok(d.exposurePolicy.allowedQualities.includes('ESTIMATED'));assert.equal(d.scoreConfidence.level,'MEDIUM');});