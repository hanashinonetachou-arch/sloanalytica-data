import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Tokyo Ghoul keeps all four Features in inference but scores only two for game-based Difficulty',()=>{const r=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/research-data.json')),s=JSON.parse(fs.readFileSync('research/L_TOKYO_GHOUL/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:200});assert.equal(d.coverage.inferenceNumericFeatureCount,4);assert.equal(d.coverage.analyzableFeatureCount,2);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);});