import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';test('Code Geass resolves Difficulty partials by explicit context exclusions',()=>{const r=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/research-data.json'));const s=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/selection-data.json'));const d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:400});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.inferenceNumericFeatureCount,3);assert.equal(d.coverage.includedNumericFeatureCount,1);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);assert.equal(d.coverage.analyzableFeatureCount,1);});