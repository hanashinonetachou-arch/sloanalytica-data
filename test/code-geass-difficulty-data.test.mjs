import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Code Geass reintroduces special-trial Features as explicit ESTIMATED exposure',()=>{const r=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/research-data.json')),s=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:250});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.inferenceNumericFeatureCount,3);assert.equal(d.coverage.analyzableFeatureCount,3);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,0);assert.equal(d.scoreConfidence.level,'LOW_MEDIUM');});