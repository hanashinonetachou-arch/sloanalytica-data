import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {evaluateMachineDifficulty} from '../tools/evaluate-machine-difficulty.mjs';
test('Code Geass keeps conditional-opportunity Features in inference but excludes them from game-based Difficulty',()=>{const r=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/research-data.json')),s=JSON.parse(fs.readFileSync('research/S_CODE_GEASS_3_CC_FS/selection-data.json')),d=evaluateMachineDifficulty(r,s,{simulationsPerSetting:250});assert.equal(d.status,'COMPLETE');assert.equal(d.coverage.inferenceNumericFeatureCount,3);assert.equal(d.coverage.analyzableFeatureCount,1);assert.equal(d.coverage.explicitlyExcludedNumericFeatureCount,2);const excluded=s.features.filter(f=>['FEAT_RB_INFINITE_AT_BINOMIAL','FEAT_AT_END_SCREEN_MULTINOMIAL'].includes(f.featureId));assert.ok(excluded.every(f=>f.difficultyParticipation==='EXCLUDE'));});